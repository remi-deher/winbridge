<Page
    x:Class="WinBridge.App.Views.TerminalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <SplitView x:Name="MainSplitView" DisplayMode="Inline" IsPaneOpen="True" OpenPaneLength="250" PaneBackground="{ThemeResource LayerFillColorDefaultBrush}">
        <SplitView.Pane>
            <Grid Padding="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Modules" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,10"/>
                <ScrollViewer Grid.Row="1">
                    <StackPanel x:Name="ModulesPanel" Spacing="10"/>
                </ScrollViewer>
            </Grid>
        </SplitView.Pane>

        <SplitView.Content>
            <Grid>
                <WebView2 x:Name="TermWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <ProgressRing x:Name="LoadingRing" IsActive="True" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </SplitView.Content>
    </SplitView>
</Page>