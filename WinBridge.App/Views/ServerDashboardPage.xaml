<Page
    x:Class="WinBridge.App.Views.ServerDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:WinBridge.App.Components"
    xmlns:sdk="using:WinBridge.SDK"
    mc:Ignorable="d"
    NavigationCacheMode="Disabled">

    <SplitView x:Name="ModuleSplitView" DisplayMode="CompactOverlay" IsPaneOpen="False" OpenPaneLength="250" CompactPaneLength="48" PaneBackground="{ThemeResource LayerFillColorDefaultBrush}">
        <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button x:Name="BtnTogglePane" Click="BtnTogglePane_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Left" Margin="6,8,0,0">
                     <FontIcon Glyph="&#xE700;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                </Button>
                
                <ListView x:Name="ModuleList" Grid.Row="1" SelectionMode="Single" SelectionChanged="ModuleList_SelectionChanged" Padding="0,12,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="sdk:ModuleAction">
                             <StackPanel Orientation="Horizontal" Spacing="16" Padding="10,8">
                                 <FontIcon Glyph="{x:Bind IconGlyph}" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                 <TextBlock Text="{x:Bind Title}" VerticalAlignment="Center"/>
                             </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </SplitView.Pane>
        <SplitView.Content>
            <Grid>
                 <Grid x:Name="DashboardView" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" x:Name="ColCenter"/>
            <ColumnDefinition Width="400" x:Name="ColRight"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" Padding="40">
                <StackPanel Spacing="24">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="8">
                            <TextBlock x:Name="TxtServerName" Text="Chargement..." Style="{StaticResource TitleTextBlockStyle}" FontSize="32"/>
                            <StackPanel Orientation="Horizontal" Spacing="8" Opacity="0.6">
                                <FontIcon Glyph="&#xE7F4;" FontFamily="Segoe MDL2 Assets"/>
                                <TextBlock x:Name="TxtServerHost" Text="..." Style="{StaticResource SubtitleTextBlockStyle}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">

                            <Button x:Name="BtnShutdown" Click="BtnShutdown_Click" ToolTipService.ToolTip="Éteindre le serveur">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE7E8;" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource SystemAccentColor}"/>
                                    <TextBlock Text="Éteindre" Foreground="{ThemeResource SystemAccentColor}"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="BtnReboot" Click="BtnReboot_Click" ToolTipService.ToolTip="Redémarrer le serveur">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE777;" FontFamily="Segoe MDL2 Assets" Foreground="#D13438"/>
                                    <TextBlock Text="Redémarrer" Foreground="#D13438"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <Rectangle Height="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Margin="0,10"/>

                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24">
                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <FontIcon Glyph="&#xE930;" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource SystemAccentColor}" FontSize="24"/>
                                <TextBlock Text="Connexion SSH Active" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            </StackPanel>
                            <TextBlock Text="Terminal prêt. Le canal de communication est sécurisé." 
                                       Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7"/>
                        </StackPanel>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <Expander Grid.Row="1" Header="Journal d'activités" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="20,0,20,20" IsExpanded="False">
                <components:LogsControl x:Name="MyLogsControl" Height="200"/>
            </Expander>
        </Grid>

        <Grid Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="350" x:Name="RowTerminal"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <components:TerminalControl x:Name="MyTerminal" Grid.Row="0" ToggleFullScreen="MyTerminal_ToggleFullScreen"/>

            <StackPanel Grid.Row="1" Padding="24" Spacing="24">
                <TextBlock Text="Métriques en direct" Style="{StaticResource SubtitleTextBlockStyle}"/>

                <Grid ColumnSpacing="12" RowSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <FontIcon Grid.Row="0" Grid.Column="0" Glyph="&#xE7F8;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Système" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblOs" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="1" Grid.Column="0" Glyph="&#xE74C;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="1" Grid.Column="1">
                        <TextBlock Text="Version du Noyau" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblKernel" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="2" Grid.Column="0" Glyph="&#xE823;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <TextBlock Text="Uptime" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblUptime" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="3" Grid.Column="0" Glyph="&#xE809;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="3" Grid.Column="1">
                        <TextBlock Text="Adresse IP" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblIp" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="4" Grid.Column="0" Glyph="&#xE9D9;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="4" Grid.Column="1">
                        <TextBlock Text="Charge CPU" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblCpu" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="5" Grid.Column="0" Glyph="&#xE967;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="5" Grid.Column="1">
                        <TextBlock Text="RAM Utilisée" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblRam" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="6" Grid.Column="0" Glyph="&#xEDA2;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="6" Grid.Column="1">
                        <TextBlock Text="Disque Principal" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblDisk" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
                 </Grid>
                 <Frame x:Name="ModuleFrame" Visibility="Collapsed"/>
            </Grid>
        </SplitView.Content>
    </SplitView>
</Page>