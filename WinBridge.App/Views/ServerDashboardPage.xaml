<Page
    x:Class="WinBridge.App.Views.ServerDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:WinBridge.App.Components"
    mc:Ignorable="d"
    NavigationCacheMode="Disabled">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" x:Name="ColCenter"/>
            <ColumnDefinition Width="400" x:Name="ColRight"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" Padding="40">
            <StackPanel Spacing="24">
                <StackPanel Spacing="8">
                    <TextBlock x:Name="TxtServerName" Text="Chargement..." Style="{StaticResource TitleTextBlockStyle}" FontSize="32"/>
                    <StackPanel Orientation="Horizontal" Spacing="8" Opacity="0.6">
                        <FontIcon Glyph="&#xE7F4;" FontFamily="Segoe MDL2 Assets"/>
                        <TextBlock x:Name="TxtServerHost" Text="..." Style="{StaticResource SubtitleTextBlockStyle}"/>
                    </StackPanel>
                </StackPanel>

                <Rectangle Height="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Margin="0,10"/>

                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24">
                    <StackPanel Spacing="16">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE930;" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource SystemAccentColor}" FontSize="24"/>
                            <TextBlock Text="Connexion SSH Active" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        </StackPanel>
                        <TextBlock Text="Le canal de communication chiffré est établi. Vous pouvez utiliser le terminal interactif sur la droite." 
                                   Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7"/>
                    </StackPanel>
                </Grid>

            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="300" x:Name="RowTerminal"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <components:TerminalControl x:Name="MyTerminal" Grid.Row="0" ToggleFullScreen="MyTerminal_ToggleFullScreen"/>

            <StackPanel Grid.Row="1" Padding="24" Spacing="24">
                <TextBlock Text="Métriques en direct" Style="{StaticResource SubtitleTextBlockStyle}"/>

                <Grid ColumnSpacing="12" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <FontIcon Grid.Row="0" Grid.Column="0" Glyph="&#xE7F8;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Système" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblOs" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="1" Grid.Column="0" Glyph="&#xE823;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="1" Grid.Column="1">
                        <TextBlock Text="Uptime" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblUptime" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>

                    <FontIcon Grid.Row="2" Grid.Column="0" Glyph="&#xEDA2;" FontFamily="Segoe MDL2 Assets" Opacity="0.5"/>
                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <TextBlock Text="Disque (Root)" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        <TextBlock x:Name="lblDisk" Text="..." FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>