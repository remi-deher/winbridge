<Page
    x:Class="WinBridge.App.Views.StorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinBridge.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WinBridge.App.Models.Store"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <!-- Header -->
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Store" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            
            <Button Grid.Column="1" Click="Refresh_Click" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;"/>
                    <TextBlock Text="Refresh"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="2" Content="Gérer les dépôts">
                 <Button.Flyout>
                     <Flyout x:Name="SourcesFlyout">
                         <StackPanel Spacing="12" Width="400">
                             <TextBlock Text="Dépôts de modules" Style="{StaticResource SubtitleTextBlockStyle}"/>
                             <ListView ItemsSource="{x:Bind StoreSources}" Height="150" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" CornerRadius="4">
                                 <ListView.ItemTemplate>
                                     <DataTemplate>
                                         <Grid Padding="0,4">
                                             <Grid.ColumnDefinitions>
                                                 <ColumnDefinition Width="*"/>
                                                 <ColumnDefinition Width="Auto"/>
                                             </Grid.ColumnDefinitions>
                                             <TextBlock Text="{Binding}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,8,0"/>
                                             <Button Grid.Column="1" Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" Click="DeleteSource_Click" Tag="{Binding}" ToolTipService.ToolTip="Supprimer"/>
                                         </Grid>
                                     </DataTemplate>
                                 </ListView.ItemTemplate>
                             </ListView>
                             
                             <Grid ColumnSpacing="8">
                                 <Grid.ColumnDefinitions>
                                     <ColumnDefinition Width="*"/>
                                     <ColumnDefinition Width="Auto"/>
                                 </Grid.ColumnDefinitions>
                                 <TextBox x:Name="NewSourceUrlBox" PlaceholderText="https://.../marketplace.json"/>
                                 <Button Grid.Column="1" Content="Ajouter" Click="AddSource_Click" Style="{StaticResource AccentButtonStyle}"/>
                             </Grid>
                         </StackPanel>
                     </Flyout>
                 </Button.Flyout>
            </Button>
        </Grid>

        <!-- Search and Filters -->
        <StackPanel Grid.Row="1" Spacing="12" Margin="0,0,0,24">
            <AutoSuggestBox x:Name="SearchBox" 
                            PlaceholderText="Search modules..." 
                            QueryIcon="Find"
                            TextChanged="SearchBox_TextChanged"/>

            <!-- Dynamic Chips for Categories -->
            <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" HorizontalScrollMode="Enabled">
                <ItemsControl x:Name="FiltersControl" ItemsSource="{x:Bind Categories, Mode=OneWay}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding}" 
                                          Click="Filter_Click" 
                                          CornerRadius="16"
                                          Padding="12,4"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>

        <!-- Content Grid -->
        <GridView x:Name="ModulesGrid" 
                  Grid.Row="2"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="ModulesGrid_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:MarketplaceModule">
                    <Grid Width="280" Height="160" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                          CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header: Icon + Name + Badge -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Remote images should be cached. Using simple Image for now. -->
                            <!-- Ideally use a cached image control or check if Image supports http url out of box in GridView -->
                            <Image Source="{x:Bind IconUrl}" Width="32" Height="32" Margin="0,0,12,0"/>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{x:Bind Author}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>

                            <!-- Compatibility Check (Simple indicator if OS matches) -->
                             <!-- We'll handle this visibility in code-behind or use converter? 
                                  For simplicity in V1, let's just show a checkmark if it's "Stable" -->
                            <Border Grid.Column="2" 
                                    Background="{ThemeResource SystemFillColorSuccessBackgroundBrush}" 
                                    CornerRadius="4" Padding="6,2"
                                    Visibility="Visible"> 
                                <TextBlock Text="Stable" Style="{StaticResource CaptionTextBlockStyle}" 
                                           Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Border>
                        </Grid>

                        <!-- Description -->
                        <TextBlock Grid.Row="1" Text="{x:Bind ShortDescription}" 
                                   Style="{StaticResource BodyTextBlockStyle}" 
                                   TextWrapping="Wrap" TextTrimming="CharacterEllipsis" 
                                   Margin="0,12,0,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <!-- Footer -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,12,0,0">
                            <!-- Categories tags could go here -->
                            <FontIcon Glyph="&#xE909;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                            <TextBlock Text="{x:Bind Version}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        
        <!-- Empty State InfoBar -->
        <InfoBar x:Name="NoModulesInfoBar" 
                 Grid.Row="2"
                 Severity="Informational" 
                 Title="No modules found"
                 Message="Ensure your repository URLs are correct in Settings."
                 IsOpen="False"
                 VerticalAlignment="Top"/>

        <!-- Loading State -->
        <ProgressRing x:Name="LoadingRing" Grid.Row="2" IsActive="False" Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock x:Name="ErrorText" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
    </Grid>
</Page>
