<Page
    x:Class="WinBridge.App.Views.ModuleDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinBridge.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WinBridge.App.Models.Store"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Breadcrumb/Back -->
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Back Button -->
        <Button Click="Back_Click" Style="{StaticResource NavigationBackButtonNormalStyle}" Margin="12,12,0,0"/>

        <!-- Header -->
        <Grid Grid.Row="1" Padding="24" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Fixed: Wrapped Image in Border for CornerRadius -->
            <Border Width="96" Height="96" CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}">
                <Image x:Name="IconImage" Stretch="UniformToFill"/>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                <TextBlock x:Name="TitleText" Style="{StaticResource DisplaySubtitleTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock x:Name="AuthorText" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="•" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="VersionText" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="2" VerticalAlignment="Center" Width="200" Spacing="8">
                <Button x:Name="InstallButton" Click="Install_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE896;" FontSize="16"/>
                        <TextBlock Text="Install"/>
                    </StackPanel>
                </Button>
                <!-- Progress -->
                <ProgressBar x:Name="InstallProgress" IsIndeterminate="True" Visibility="Collapsed"/>
                <TextBlock x:Name="StatusText" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <!-- Content Body -->
        <Grid Grid.Row="2" Padding="24,0,24,24" ColumnSpacing="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Main Info -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="24">
                    <!-- Gallery -->
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" HorizontalScrollMode="Enabled">
                        <ItemsControl x:Name="ScreenshotsList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="12"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- Fixed: Wrapped Image in Border for styling -->
                                    <Border CornerRadius="4" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1">
                                        <Image Source="{Binding}" Height="200" Stretch="Uniform"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Description -->
                    <TextBlock Text="Description" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock x:Name="DescriptionText" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Sidebar -->
            <StackPanel Grid.Column="1" Spacing="24">
                
                <!-- Permissions -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Permissions" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <ItemsControl x:Name="PermissionsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4">
                                    <!-- Fixed: Removed FluentTheme property -->
                                    <FontIcon Glyph="&#xE72E;" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding}" Style="{StaticResource BodyTextBlockStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <MenuFlyoutSeparator/>

                <!-- Meta -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Release Date" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <TextBlock x:Name="DateText" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <StackPanel Spacing="8">
                     <HyperlinkButton x:Name="WebsiteLink" Content="Visit Website" NavigateUri="https://github.com"/>
                </StackPanel>

            </StackPanel>
        </Grid>
    </Grid>
</Page>
<!-- Force IDE Refresh -->
