<Page
    x:Class="WinBridge.App.Views.AddServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Padding="40" Spacing="10" MaxWidth="500" HorizontalAlignment="Left">
        <TextBlock Text="Ajouter un nouveau serveur" Style="{StaticResource HeaderTextBlockStyle}"/>

        <TextBox x:Name="TxtName" Header="Nom du serveur (ex: Web-Prod)"/>
        <TextBox x:Name="TxtHost" Header="Adresse IP ou Hostname"/>
        <controls:NumberBox x:Name="NumPort" Header="Port" Value="22"/>
        <TextBox x:Name="TxtUser" Header="Utilisateur"/>

        <PasswordBox x:Name="TxtPassword" Header="Mot de passe"/>

        <Button Content="Enregistrer le serveur" 
                Click="BtnSave_Click" 
                Style="{StaticResource AccentButtonStyle}"
                Margin="0,20,0,0"/>
    </StackPanel>
</Page>