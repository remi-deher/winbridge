<Page
    x:Class="WinBridge.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WinBridge.Models.Entities"
    xmlns:converters="using:WinBridge.App.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:LastSeenToColorConverter x:Key="LastSeenToColorConverter"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer Padding="40">
            <StackPanel Spacing="32" MaxWidth="1200" HorizontalAlignment="Left">

                <StackPanel Spacing="4">
                    <TextBlock x:Name="TxtGreeting" Text="Bonjour," Style="{StaticResource TitleTextBlockStyle}" FontSize="32"/>
                    <TextBlock x:Name="TxtDate" Text="Nous sommes le ..." Style="{StaticResource SubtitleTextBlockStyle}" Opacity="0.6"/>
                </StackPanel>

                <Grid ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Tapped="CardServers_Tapped" PointerEntered="Card_PointerEntered" PointerExited="Card_PointerExited"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24" Height="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Serveurs gérés" Opacity="0.6" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <TextBlock x:Name="TxtServerCount" Text="0" FontSize="28" FontWeight="SemiBold"/>
                        </StackPanel>
                        <FontIcon Grid.Column="1" Glyph="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="32" Opacity="0.2"/>
                    </Grid>

                    <Grid Grid.Column="1" Tapped="CardKeys_Tapped" PointerEntered="Card_PointerEntered" PointerExited="Card_PointerExited"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24" Height="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Clés SSH Sécurisées" Opacity="0.6" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <TextBlock x:Name="TxtKeyCount" Text="0" FontSize="28" FontWeight="SemiBold"/>
                        </StackPanel>
                        <FontIcon Grid.Column="1" Glyph="&#xE898;" FontFamily="Segoe MDL2 Assets" FontSize="32" Opacity="0.2"/>
                    </Grid>
                </Grid>

                <Grid ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                          CornerRadius="8" Padding="20">
                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE9ca;" FontFamily="Segoe MDL2 Assets" Opacity="0.6"/>
                                <TextBlock Text="Systèmes d'exploitation" FontWeight="SemiBold"/>
                            </StackPanel>

                            <Grid x:Name="GridChartContainer" Height="150">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Canvas x:Name="ChartCanvas" Width="150" Height="150" HorizontalAlignment="Left"/>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock x:Name="TxtTotalServers" Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Total" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <ScrollViewer Grid.Column="1" Margin="20,0,0,0" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="LegendStack" Spacing="8" VerticalAlignment="Center"/>
                                </ScrollViewer>
                            </Grid>

                            <TextBlock x:Name="TxtNoOsData" Text="Pas assez de données." Opacity="0.4" FontSize="12" Visibility="Collapsed" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                          CornerRadius="8" Padding="20">
                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE7BA;" FontFamily="Segoe MDL2 Assets" Opacity="0.6"/>
                                <TextBlock Text="Alertes de Disponibilité" FontWeight="SemiBold"/>
                            </StackPanel>

                            <ScrollViewer MaxHeight="150">
                                <StackPanel x:Name="PanelAlerts" Spacing="8"/>
                            </ScrollViewer>

                            <TextBlock x:Name="TxtAllGood" Text="Tout va bien ! Tous les serveurs ont été vus récemment." 
                                       Opacity="0.5" FontStyle="Italic" Visibility="Collapsed" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Grid>

                <StackPanel Spacing="16">
                    <TextBlock Text="Accès Rapide" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <GridView x:Name="GridServers" SelectionMode="None" IsItemClickEnabled="True" ItemClick="GridServers_ItemClick">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Margin" Value="0,0,12,12"/>
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="models:ServerModel">
                                <Grid Width="260" Height="110" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                                      CornerRadius="8" Padding="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0" Width="48" Height="48" VerticalAlignment="Top">
                                        <Grid Width="48" Height="48" CornerRadius="24" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                            <FontIcon Glyph="&#xE7F8;" FontFamily="Segoe MDL2 Assets" FontSize="20"/>
                                        </Grid>
                                        <Ellipse Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Bottom" StrokeThickness="2" Stroke="{ThemeResource LayerFillColorDefaultBrush}"
                                                 Fill="{x:Bind LastSeen, Converter={StaticResource LastSeenToColorConverter}, Mode=OneWay}"/>
                                    </Grid>
                                    <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{x:Bind CachedIpAddress, TargetNullValue='IP Inconnue', FallbackValue='...'}" Opacity="0.8" FontSize="12" Margin="0,2,0,0"/>
                                        <TextBlock Text="{x:Bind CachedOsInfo, TargetNullValue='OS Inconnu', FallbackValue='Linux'}" Opacity="0.5" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                        <TextBlock Text="{x:Bind CachedUptime, TargetNullValue='Jamais connecté', FallbackValue='...'}" Opacity="0.4" FontSize="10" Margin="0,8,0,0" FontStyle="Italic"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                    <TextBlock x:Name="TxtNoServers" Text="Aucun serveur configuré." Opacity="0.5" Visibility="Collapsed"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>