<Page
    x:Class="WinBridge.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tableau de bord" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>

        <GridView Grid.Row="1" SelectionMode="None">
            <GridViewItem>
                <Grid Width="240" Height="140" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                    <StackPanel VerticalAlignment="Center" Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="32" Foreground="{ThemeResource SystemAccentColor}"/>
                            <TextBlock Text="Serveurs" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        </StackPanel>
                        <TextBlock x:Name="TxtServerCount" Text="Chargement..." Style="{StaticResource BodyStrongTextBlockStyle}" FontSize="24" Margin="44,0,0,0"/>
                    </StackPanel>
                </Grid>
            </GridViewItem>

            <GridViewItem>
                <Grid Width="240" Height="140" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                    <StackPanel VerticalAlignment="Center" Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE8D7;" FontFamily="Segoe MDL2 Assets" FontSize="32" Foreground="{ThemeResource SystemAccentColor}"/>
                            <TextBlock Text="Clés SSH" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        </StackPanel>
                        <TextBlock x:Name="TxtKeyCount" Text="Chargement..." Style="{StaticResource BodyStrongTextBlockStyle}" FontSize="24" Margin="44,0,0,0"/>
                    </StackPanel>
                </Grid>
            </GridViewItem>
        </GridView>
    </Grid>
</Page>