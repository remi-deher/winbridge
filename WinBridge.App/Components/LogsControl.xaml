<UserControl
    x:Class="WinBridge.App.Components.LogsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinBridge.App.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:broadcasting="using:WinBridge.SDK.Broadcasting"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:LogLevelToColorConverter x:Key="LogLevelToColorConverter"/>
        <local:DateTimeFormatConverter x:Key="DateTimeFormatConverter"/>
    </UserControl.Resources>

    <Grid Background="#1E1E1E" RequestedTheme="Dark">
        <TabView IsAddTabButtonVisible="False" TabStripHeader="Logs">
            <TabViewItem Header="Serveur" IsClosable="False">
                <TabViewItem.IconSource>
                    <SymbolIconSource Symbol="Remote"/>
                </TabViewItem.IconSource>
                <ListView x:Name="ListServerLogs" ItemsSource="{x:Bind ServerLogs, Mode=OneWay}" SelectionMode="None" Background="#1E1E1E">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="broadcasting:LogMessage">
                            <TextBlock TextWrapping="Wrap" Foreground="{x:Bind Level, Converter={StaticResource LogLevelToColorConverter}}">
                                <Run Text="["/><Run Text="{x:Bind Timestamp, Converter={StaticResource DateTimeFormatConverter}}"/><Run Text="] "/>
                                <Run Text="{x:Bind Message}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabViewItem>
            
            <TabViewItem Header="Global" IsClosable="False">
                <TabViewItem.IconSource>
                    <SymbolIconSource Symbol="World"/>
                </TabViewItem.IconSource>
                 <ListView x:Name="ListGlobalLogs" ItemsSource="{x:Bind AllLogs, Mode=OneWay}" SelectionMode="None" Background="#1E1E1E">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="broadcasting:LogMessage">
                            <TextBlock TextWrapping="Wrap" Foreground="{x:Bind Level, Converter={StaticResource LogLevelToColorConverter}}">
                                <Run Text="["/><Run Text="{x:Bind Timestamp, Converter={StaticResource DateTimeFormatConverter}}"/><Run Text="] "/>
                                <Run Text="{x:Bind Message}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabViewItem>
        </TabView>
    </Grid>
</UserControl>
